[package]
name = "plato-room-nav"
version = "0.1.0"
edition = "2021"
description = "Room navigation engine — pathfinding, room graph traversal, shortest routes, discovery"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
