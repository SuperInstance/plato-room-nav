[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-nav"
version = "0.1.0"
description = "Room navigation — breadcrumb trails, push/back/forward history"
license = {text = "MIT"}
requires-python = ">=3.8"
